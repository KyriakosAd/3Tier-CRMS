import { RoomAvailability } from './room-availability.model';

describe('RoomAvailability', () => {
  it('should create an instance', () => {
    expect(new RoomAvailability()).toBeTruthy();
  });
});
